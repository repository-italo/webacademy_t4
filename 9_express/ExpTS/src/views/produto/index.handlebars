<div class="d-flex justify-content-between">
  <h3>Listagem de Produtos</h3>
  <a href="produto/create"><button class="btn btn-primary">+</button></a>
</div>

<table class="table">
  <thead>
    <tr>
      <th scope="col">ID</th>
      <th scope="col">Nome</th>
      <th scope="col">Preço</th>
      <th scope="col">Estoque</th>

    </tr>
  </thead>
  <tbody>
    {{#each produtos}}
    <tr>
      <th scope="row">{{id}}</th>
      <td>{{nome}}</td>
      <td>{{preco}}</td>
      <td>{{estoque}}</td>
      <td><a href="/produto/update/{{id}}"><i class="bi bi-pencil-square"></i></a></td>
      <td><a href="/produto/read/{{id}}"><i class="bi bi-eye-fill"></i></a></td>
      <td><a href="/produto/delete/{{id}}"><i class="bi bi-trash-fill"></i></a></td>
    </tr>
    {{/each}}
  </tbody>
</table>
